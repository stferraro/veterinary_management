<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_pet_consultation_calendar" model="ir.ui.view">
            <field name="name">pet.consultation.calendar</field>
            <field name="model">pet.consultation</field>
            <field name="arch" type="xml">
                <calendar string="Pet Consultations" date_start="date" color="state">
                    <field name="reference"/>
                    <field name="name"/>
                    <field name="pet_id"/>
                    <field name="veterinarian_id"/>
                    <field name="state"/>
                </calendar>
            </field>
        </record>

        <record id="view_pet_consultation_form" model="ir.ui.view">
            <field name="name">pet.consultation.form</field>
            <field name="model">pet.consultation</field>
            <field name="arch" type="xml">
                <form string="Pet Consultation">
                    <sheet>
                        <group>
                            <field name="reference"/>
                            <field name="name"/>
                            <field name="date"/>
                            <field name="pet_id"/>
                            <field name="veterinarian_id"/>
                        </group>
                        <notebook>
                            <page string="Treatments">
                                <field name="treatment_ids">
                                    <list editable="bottom">
                                        <field name="product_id"/>
                                        <field name="quantity"/>
                                        <field name="unit_price"/>
                                        <field name="subtotal" readonly="1"/>
                                    </list>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="action_pet_consultation" model="ir.actions.act_window">
            <field name="name">Pet Consultations Calendar</field>
            <field name="res_model">pet.consultation</field>
            <field name="view_mode">calendar,list,form</field>
            <field name="view_id" ref="view_pet_consultation_calendar"/>
        </record>


    </data>
</odoo>